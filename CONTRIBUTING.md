# 贡献指南

感谢你愿意改进聊天截图生成器。提交前请先阅读 [README.md](README.md) 了解当前功能、架构、数据边界和验证方式。

## 开发环境

- Node.js 22.12 或更高版本。
- 使用仓库中的 `package-lock.json` 和 npm 安装依赖。
- 当前浏览器回归基线为桌面版 Google Chrome。

```bash
npm ci
npm run dev
```

项目是纯前端本地工具。除非变更经过单独讨论，请勿增加后端、云上传、账号、遥测、真实支付或通信能力。

## 提交修改

1. 先创建或关联 Issue，说明用户问题、预期行为和影响范围。
2. 从最新代码创建小而聚焦的分支。
3. 修复缺陷或改变行为时补充对应测试。
4. 同步更新受影响的 README 内容。
5. 提交前运行完整检查：

```bash
npm run check
npm run test:e2e
```

不要提交 `node_modules/`、`dist/`、测试报告、个人聊天素材或项目 JSON。新增媒体素材时，请同时确认体积和导出行为。

## 代码约定

- 保持 React、TypeScript、Vite 和 CSS Modules 技术栈。
- 预览与 PNG 必须继续共用 `ChatCanvas`，预览缩放不能改变输出尺寸。
- 草稿修改通过 reducer 完成，并保持批量操作可撤销。
- 新增字段或消息类型时，同步检查默认值、迁移、编辑、渲染、质量检查、项目 JSON、媒体回收和测试。
- 保留导出前的字体与图片等待、滚动冻结和失败后的状态恢复。
- 不在日志、Issue、测试夹具或截图中提交真实私人聊天内容。

## Pull Request

Pull Request 请说明：

- 解决了什么用户问题。
- 主要实现和兼容性影响。
- 实际执行的验证命令及结果。
- 界面变更的桌面、窄屏截图；画布变更还需包含浅色、深色和实际 PNG。

提交贡献即表示你有权提交相关内容，并同意将你贡献的项目代码按仓库的 MIT 许可证发布。
